{
  "name": "04-pre-post",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src -d lib",
    "cleanup": "rimraf lib",
    "prebuild": "npm run cleanup"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "rimraf": "^2.5.4"
  }
}
